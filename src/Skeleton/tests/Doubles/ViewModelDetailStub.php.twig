<?php declare(strict_types=1);
{% include "Header.php.twig" %}
{% import _self as macro %}

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.parentClassName }};

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
abstract class {{ skeletonModel.shortName }} extends {{ skeletonModel.parentShortName }}
{
{% for field in skeletonModel.fields %}
    const {{ field.const }} = {{ field.value }};
{% if not loop.last %}

{% endif %}
{% endfor %}

{% for field in skeletonModel.fields %}
    /**
    * {@inheritdoc}
    */
    {{ field.scope }} ${{ field.name }} = self::{{ field.const }};
{% if not loop.last %}

{% endif %}
{% endfor %}
}
