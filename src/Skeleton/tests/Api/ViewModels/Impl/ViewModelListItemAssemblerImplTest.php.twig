{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.viewModelListItemAssemblerClassName }};
use {{ skeletonModel.viewModelListItemAssemblerImplClassName }};
use {{ skeletonModel.viewModelListItemStubClassName }};
use {{ skeletonModel.viewModelListItemTestCaseClassName }};
use {{ skeletonModel.useCaseListItemResponseStubClassName }};
use PHPUnit\Framework\TestCase;

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
class {{ skeletonModel.shortName }} extends TestCase
{
    use {{ skeletonModel.viewModelListItemTestCaseShortName }};

    /**
     * @var {{ skeletonModel.viewModelListItemAssemblerShortName }}
     */
    private $assembler;

    /**
     * @test
     */
    public function onCreateShouldReturnViewModel()
    {
        $actual = $this->assembler->createListItems([new {{ skeletonModel.useCaseListItemResponseStubShortName }}()]);
        $expected = [new {{ skeletonModel.viewModelListItemStubShortName }}()];
        $this->{{ skeletonModel.viewModelListItemTestCaseMethod }}($expected, $actual);
    }

    protected function setUp()
    {
        $this->assembler = new {{ skeletonModel.viewModelListItemAssemblerImplShortName }}();
    }
}
