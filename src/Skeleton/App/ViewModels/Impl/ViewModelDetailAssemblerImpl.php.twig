<?php
{% include "Header.php.twig" %}

namespace {{ viewModelDetailAssemblerImplNamespace }};

use {{ viewModelAssemblerTraitClassName }};
use {{ viewModelDetailClassName }};
use {{ viewModelDetailAssemblerClassName }};
use {{ useCaseResponseClassName }};

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
class {{ viewModelDetailAssemblerImplShortClassName }} implements {{ viewModelDetailAssemblerShortClassName }}
{
    use {{ viewModelAssemblerTraitShortClassName }};

    public function createDetail({{ useCaseResponseShortClassName }} ${{ entityName }}): {{ viewModelDetailShortClassName }}
    {
        $vm = new {{ viewModelDetailImplShortClassName }}();
        $this->hydrateCommonFields($vm, ${{ entityName }});

        return $vm;
    }
}
