<?php
{% include "Header.php.twig" %}

namespace {{ listViewModelImplNamespace }};

use {{ listViewModelClassName }};

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
abstract class {{ listViewModelImplShortClassName }} extends {{ listViewModelShortClassName }}
{
{% for field in fields %}
    {{ field.docComment }}
    {{ field.scope }} ${{ field.name }};

{% endfor %}
}
