<?php
{% include "Header.php.twig" %}

namespace {{ listViewModelNamespace }};

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
abstract class {{ listViewModelShortClassName }}
{
{% for field in fields %}
    {{ field.docComment }}
    {{ field.scope }} ${{ field.name }};

{% endfor %}
}
