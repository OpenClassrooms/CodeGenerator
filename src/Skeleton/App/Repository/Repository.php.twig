<?php
{% include "Header.php.twig" %}

namespace {{ repositoryNamespace }};

use Doctrine\ORM\EntityRepository;
use {{ gatewayClassName }};
use {{ entityNotFoundExceptionClassName }};

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
class {{ repositoryShortClassName }} extends EntityRepository implements {{ gatewayShortClassName }}
{
{% if find %}
    /**
     * {@inheritdoc}
     */
    public function find($id)
    {
        ${{ entityFieldName }} = parent::find($id);
        if (null === ${{ entityFieldName }}) {
            throw new {{ entityNotFoundExceptionShortClassName }}($id);
        }

        return ${{ entityFieldName }};
    }
{% endif %}
}
