{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

use OC\BusinessRules\Utils\DtoCommonFieldsHydrator;
use {{ skeletonModel.useCaseDetailResponseClassName }};

class {{ skeletonModel.shortName }}
{
    public function create({{ skeletonModel.useCaseDetailResponseShortName }} ${{ skeletonModel.useCaseDetailResponseArgument}}): {{ skeletonModel.viewModelDetailShortName }}
    {
        $vm = new {{ skeletonModel.viewModelDetailShortName }}();
        DtoCommonFieldsHydrator::hydrate($vm, ${{ skeletonModel.useCaseDetailResponseArgument}});

        return $vm;
    }
}
