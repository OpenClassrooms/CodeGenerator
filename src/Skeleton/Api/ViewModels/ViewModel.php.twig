{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

abstract class {{ skeletonModel.shortName }}
{
{% for field in skeletonModel.fields %}
    {{ field.scope }} {{ field.type }} ${{ field.name }};
{% if not loop.last %}

{% endif %}
{% endfor %}
}
