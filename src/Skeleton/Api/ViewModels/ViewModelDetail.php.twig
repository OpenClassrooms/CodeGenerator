<?php declare(strict_types=1);
{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
abstract class {{ skeletonModel.shortClassName }} extends {{ skeletonModel.ParentShortClassName }}
{
{% for field in skeletonModel.fields %}
    {{ field.docComment }}
    {{ field.scope }} ${{ field.name }};
{% if not loop.last %}

{% endif %}
{% endfor %}
}
