{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.viewModelListItemClassName }};
use PHPUnit\Framework\Assert as Assert;

trait {{ skeletonModel.shortName }}
{
    use {{ skeletonModel.viewModelTestCaseShortName }};

    /**
     * @param {{ skeletonModel.viewModelListItemShortName }}[] $expected
     * @param {{ skeletonModel.viewModelListItemShortName }}[] $actual
     */
    protected function assert{{ skeletonModel.viewModelListItemShortName }}s(array $expected, array $actual): void
    {
        Assert::assertCount(count($expected), $actual);
        foreach ($expected as $key => $item) {
            $this->assert{{ skeletonModel.viewModelListItemShortName }}($item, $actual[$key]);
        }
    }

    private function assert{{ skeletonModel.viewModelListItemShortName }}({{ skeletonModel.viewModelListItemShortName }} $expected, {{ skeletonModel.viewModelListItemShortName }} $actual): void
    {
        $this->assert{{ skeletonModel.viewModelTestCaseShortName }}($expected, $actual);
    }
}
