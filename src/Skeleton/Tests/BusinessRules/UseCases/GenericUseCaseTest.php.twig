{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.genericUseCaseRequestClassName }};
use {{ skeletonModel.genericUseCaseClassName }};
use PHPUnit\Framework\TestCase;

final class {{ skeletonModel.shortName }} extends TestCase
{
    private {{ skeletonModel.genericUseCaseRequestShortName }} $request;

    private {{ skeletonModel.genericUseCaseShortName }} $useCase;

    /** @test */
    public function {{ skeletonModel.genericUseCaseTestMethod }}ReturnResponse(): void
    {
        $this->fail("TODO: Implement this test");
    }

    protected function setUp(): void
    {
        $this->request = $this->buildRequest();
        $this->useCase = new {{ skeletonModel.genericUseCaseShortName }}();
    }

    private function buildRequest(): {{ skeletonModel.genericUseCaseRequestShortName }}
    {
        return {{ skeletonModel.genericUseCaseRequestShortName }}::create();
    }
}
