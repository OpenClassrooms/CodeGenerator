{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.viewModelDetailAssemblerClassName }};
use {{ skeletonModel.viewModelDetailStubClassName }};
use {{ skeletonModel.viewModelDetailTestCaseClassName }};
use {{ skeletonModel.useCaseDetailResponseStubClassName }};
use PHPUnit\Framework\TestCase;

final class {{ skeletonModel.shortName }} extends TestCase
{
    use {{ skeletonModel.viewModelDetailTestCaseShortName }};

    private {{ skeletonModel.viewModelDetailAssemblerShortName }} $assembler;

    /** @test */
    public function onCreateShouldReturnViewModel(): void
    {
        $actual = $this->assembler->create(new {{ skeletonModel.useCaseDetailResponseStubShortName }}());
        $expected = new {{ skeletonModel.viewModelDetailStubShortName }}();
        $this->{{ skeletonModel.viewModelDetailTestCaseMethod }}($expected, $actual);
    }

    protected function setUp(): void
    {
        $this->assembler = new {{ skeletonModel.viewModelDetailAssemblerShortName }}();
    }
}
