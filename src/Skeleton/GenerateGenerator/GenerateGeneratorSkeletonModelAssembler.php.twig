<?php declare(strict_types=1);

namespace {{ skeletonModel.baseNamespace }}SkeletonModels\{{ skeletonModel.domain }};

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
interface {{ skeletonModel.entity }}SkeletonModelAssembler
{
    public function create(): {{ skeletonModel.entity }}SkeletonModel;
}
