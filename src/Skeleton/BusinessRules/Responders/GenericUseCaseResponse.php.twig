{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.useCaseResponseClassName }};

/**
* @author {{ author }} <{{ authorEmail }}>
*/
interface {{ skeletonModel.shortName }} extends {{ skeletonModel.useCaseResponseShortName }}
{
{% for method in skeletonModel.genericUseCaseResponseMethods | sortNameByAlpha %}
{% if method.docComment %}
        {{ method.docComment }}
{% endif %}
    public function {{ method.name }}(): {{ method.returnType }};
{% if not loop.last %}

{% endif %}
{% endfor %}
}
