{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.useCaseRequestClassName }};

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
interface {{ skeletonModel.shortName }} extends {{ skeletonModel.useCaseRequestShortName }}
{
    public function create(): {{ skeletonModel.shortName }};

    public function withFilters(array $filters = []): {{ skeletonModel.shortName }};

    public function withItemsPerPage(int $itemsPerPage): {{ skeletonModel.shortName }};

    public function withPage(int $page = 1): {{ skeletonModel.shortName }};

    public function withSort(array $sort = []): {{ skeletonModel.shortName }};

    public function build(): {{ skeletonModel.getEntityUseCaseRequestShortName }};
}
