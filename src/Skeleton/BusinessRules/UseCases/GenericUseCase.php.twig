{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.genericUseCaseRequestClassName }};
use {{ skeletonModel.securityClassName }};
use {{ skeletonModel.useCaseClassName }};
use {{ skeletonModel.useCaseRequestClassName }};

class {{ skeletonModel.shortName }} implements UseCase
{
    /**
     * @Security(roles="")
     *
     * @param {{ skeletonModel.genericUseCaseRequestShortName }} ${{ skeletonModel.useCaseRequestArgument }}
     */
    public function execute({{ skeletonModel.useCaseRequestShortName }} ${{ skeletonModel.useCaseRequestArgument }})
    {
        // TODO: Implement execute() method.
    }
}
