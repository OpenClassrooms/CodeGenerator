{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.deleteEntityUseCaseRequestClassName }};

final class {{ skeletonModel.shortName }} implements {{ skeletonModel.deleteEntityUseCaseRequestShortName }}
{
    public int ${{ skeletonModel.entityIdArgument }};

    public function __construct(int $id)
    {
        $this->{{ skeletonModel.entityIdArgument }} = $id;
    }

    public function {{ skeletonModel.getEntityIdMethod }}(): int
    {
        return $this->{{ skeletonModel.entityIdArgument }};
    }
}
