<?php declare(strict_types=1);
// Auto Generated by OpenClassrooms Code Generator

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.entityClassName }};
use {{ skeletonModel.genericUseCaseDetailResponseClassName }};
use {{ skeletonModel.genericUseCaseDetailResponseAssemblerClassName }};

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
class {{ skeletonModel.shortName }} implements {{ skeletonModel.genericUseCaseDetailResponseAssemblerShortName }}
{
    use {{ skeletonModel.genericUseCaseResponseTrait }};

    /**
     * {@inheritdoc}
     */
    public function create({{ skeletonModel.entityShortName }} $entity): {{ skeletonModel.genericUseCaseDetailResponseShortName }}
    {
        $response = new {{ skeletonModel.genericUseCaseDetailResponseDTOShortName }}();

        $this->hydrateCommonFields($entity, $response);
{% for method in skeletonModel.entityMethods | sortNameByAlpha %}
        $response->{{ method.fieldName }} = $entity->{{ method.name }}();
{% endfor %}

        return $response;
    }
}
