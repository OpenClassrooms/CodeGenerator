{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.entityClassName }};
use {{ skeletonModel.genericUseCaseResponseClassName }};

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
trait {{ skeletonModel.shortName }}
{
    /**
     * @param {{ skeletonModel.genericUseCaseResponseDTOShortName }} $response
     *
     * @return {{ skeletonModel.genericUseCaseResponseShortName }}
     */
    private function hydrateCommonFields({{ skeletonModel.entityShortName }} $entity, {{ skeletonModel.genericUseCaseResponseShortName }} $response): {{ skeletonModel.genericUseCaseResponseShortName }}
    {
{% for method in skeletonModel.entityMethods %}
        $response->{{ method.fieldName }} = $entity->{{ method.name }}();
{% endfor %}

        return $response;
    }
}
