{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.useCaseDetailResponseClassName }};

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
class {{ skeletonModel.shortName }} extends {{ skeletonModel.useCaseResponseDTOShortName }} implements {{ skeletonModel.useCaseDetailResponseShortName }}
{
{% for field in skeletonModel.fields | sortNameByAlpha %}
    {{ field.docComment }}
    public ${{ field.name }};
{% if not loop.last %}

{% endif %}
{% endfor %}

{% for method in skeletonModel.methods | sortNameByAlpha %}
{% if method.docComment %}
        {{ method.docComment }}
{% endif %}
    public function {{ method.name }}(): {{ printReturnType(method.returnType, method.Nullable) }}
    {
        return $this->{{ method.fieldName }};
    }
{% if not loop.last %}

{% endif %}
{% endfor %}
}
