{% include "Header.php.twig" %}

namespace {{ skeletonModel.namespace }};

use {{ skeletonModel.entityClassName }};
use {{ skeletonModel.useCaseResponseClassName }};

trait {{ skeletonModel.shortName }}
{
    private function hydrateCommonFields({{ skeletonModel.entityShortName }} $entity, {{ skeletonModel.useCaseResponseShortName }} $response): {{ skeletonModel.useCaseResponseShortName }}
    {
{% for method in skeletonModel.entityMethods|sortAccessorsFieldNameByAlpha %}
        $response->{{ method.accessorName }} = $entity->{{ method.name }}();
{% endfor %}

        return $response;
    }
}
