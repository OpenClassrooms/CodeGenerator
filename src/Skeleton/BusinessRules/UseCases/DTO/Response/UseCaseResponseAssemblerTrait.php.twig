<?php
{% include "Header.php.twig" %}

namespace {{ useCaseResponseAssemblerTraitNamespace }};

use {{ entityClassName }};
use {{ useCaseResponseClassName }};

/**
 * @author {{ author }} <{{ authorEmail }}>
 */
trait {{ useCaseResponseAssemblerTraitShortClassName }}
{
    /**
     * @param \{{ useCaseResponseDTOClassName }}|{{ useCaseResponseShortClassName }} $response
     */
    protected function hydrateCommonFields({{ useCaseResponseShortClassName }} $response, {{ entityShortClassName }} ${{ entityFieldName }})
    {
{% for field in fields %}
        $response->{{ field.name }} = ${{ entityFieldName }}->{{ field.accessor }}();
{% endfor %}
    }
}
